cloud:
  aws:
    s3:
      bucket: ${S3_BUCKET}
    credentials:
      access-key: ${S3_ACCESS_KEY}
      secret-key: ${S3_SECRET_KEY}
      instance-profile: true
    region:
      static: ap-northeast-2
    stack:
      auto: false